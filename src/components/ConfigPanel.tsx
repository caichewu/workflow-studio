import { Activity } from 'lucide-react';
import { MODEL_OVERRIDE_DEFAULT, useWorkflowStore } from '../store';
import { modelLabelMap, modelOptions } from '../constants/models';
import { resolveAgentModel } from '../utils/resolveAgentModel';

export function ConfigPanel() {
  const agents = useWorkflowStore((s) => s.agents);
  const selectedNodeId = useWorkflowStore((s) => s.selectedNodeId);
  const selectedNode = useWorkflowStore((s) => s.nodes.find((node) => node.id === selectedNodeId) ?? null);
  const updateNode = useWorkflowStore((s) => s.updateNode);
  const selectedAgent = agents.find((agent) => agent.id === selectedNode?.data.agentId) ?? null;
  const actualModel = selectedNode?.type === 'agent' ? resolveAgentModel(selectedNode.data, selectedAgent) : null;

  if (!selectedNode) return <aside className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="rounded-2xl border border-dashed border-slate-200 bg-slate-50 p-6 text-sm text-slate-400">请选择一个节点进行编辑。</div></aside>;

  return <aside className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center gap-2 text-sm font-medium"><Activity className="h-4 w-4" /> 配置面板</div><div className="space-y-4 rounded-2xl border border-slate-200 bg-slate-50 p-4 text-sm"><div><div className="mb-1 text-xs text-slate-400">节点名称</div><input className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2" value={selectedNode.data.title} onChange={(e) => updateNode(selectedNode.id, { title: e.target.value })} /></div><div><div className="mb-1 text-xs text-slate-400">节点描述</div><textarea className="min-h-20 w-full rounded-xl border border-slate-200 bg-white px-3 py-2" value={selectedNode.data.description} onChange={(e) => updateNode(selectedNode.id, { description: e.target.value })} /></div>{selectedNode.type === 'agent' && <><div><div className="mb-1 text-xs text-slate-400">绑定 Agent</div><select className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2" value={selectedNode.data.agentId ?? agents[0]?.id ?? ''} onChange={(e) => updateNode(selectedNode.id, { agentId: e.target.value })}>{agents.map((agent) => <option key={agent.id} value={agent.id}>{agent.name}</option>)}</select></div><div><div className="mb-1 text-xs text-slate-400">覆盖模型</div><select className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2" value={selectedNode.data.overrideModel ?? MODEL_OVERRIDE_DEFAULT} onChange={(e) => updateNode(selectedNode.id, { overrideModel: e.target.value })}>{modelOptions.map((model) => <option key={model} value={model}>{modelLabelMap[model] ?? model}</option>)}</select></div><div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-600"><div className="font-medium text-cyan-600">{selectedAgent?.name ?? '未绑定 Agent'}</div><div className="mt-1">{selectedAgent?.description}</div><div className="mt-2 text-slate-500">默认模型：{selectedAgent?.model ?? '未设置'}</div><div className="mt-1 text-slate-700">当前实际模型：{actualModel ?? '未设置'}</div></div></>}{selectedNode.type === 'condition' && <div><div className="mb-1 text-xs text-slate-400">条件表达式</div><input className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2" value={selectedNode.data.condition ?? ''} onChange={(e) => updateNode(selectedNode.id, { condition: e.target.value })} /></div>}{selectedNode.type === 'api' && <div className="grid grid-cols-2 gap-2"><div><div className="mb-1 text-xs text-slate-400">请求方法</div><select className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2" value={selectedNode.data.method ?? 'GET'} onChange={(e) => updateNode(selectedNode.id, { method: e.target.value })}><option>GET</option><option>POST</option><option>PUT</option><option>DELETE</option></select></div><div><div className="mb-1 text-xs text-slate-400">API 地址</div><input className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2" value={selectedNode.data.url ?? ''} onChange={(e) => updateNode(selectedNode.id, { url: e.target.value })} /></div></div>}{selectedNode.type === 'code' && <div><div className="mb-1 text-xs text-slate-400">代码</div><textarea className="min-h-28 w-full rounded-xl border border-slate-200 bg-white px-3 py-2 font-mono text-xs" value={selectedNode.data.code ?? ''} onChange={(e) => updateNode(selectedNode.id, { code: e.target.value })} /></div>}<div className="text-xs text-slate-500">节点类型：{selectedNode.type}</div></div></aside>;
}
